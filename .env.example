VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
# Telegram bot token is a server-side secret — set it via: supabase secrets set TELEGRAM_BOT_TOKEN=your_bot_token
# Never prefix it with VITE_ or it will be bundled into the client JS bundle
